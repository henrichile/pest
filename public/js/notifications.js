function markAsRead(notificationId) { fetch(, { method: "POST", headers: { "Content-Type": "application/json", "X-CSRF-TOKEN": document.querySelector("meta[name=csrf-token]").getAttribute("content") } }).then(response => { if (response.ok) { location.reload(); } }); } setInterval(function() { fetch("/notifications/count").then(response => response.json()).then(data => { const badge = document.querySelector(".notification-badge"); if (badge) { badge.textContent = data.count; } }); }, 30000);
